ONTO — PRE-LAUNCH CHECKLIST UPDATE
Session: 2026-03-30 (continued)
Previous version: v5 (38 remaining)
This version: v6

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CHANGES FROM v5 TO v6

1. MARK COMPLETE — Item 1.12
   Change: [ ] 1.12 → [✅] 1.12
   Note: 16 conformance tests added in tests/test_conformance.py
         All three ratification conditions now fully met.

2. MARK COMPLETE — Item 2.03
   Change: [ ] 2.03 → [✅] 2.03
   Note: Already completed as part of items 1.07 and 1.08.
         SQL injection, command injection, path traversal all handled.
         Formally closed this session.

3. MARK COMPLETE — Item 2.05
   Change: [ ] 2.05 → [✅] 2.05
   Note: core/ratelimit.py — sliding window rate limiter.
         Default: 60 inputs per 60 seconds. Configurable via env.
         Tests in tests/test_security.py.

4. MARK COMPLETE — Item 2.06
   Change: [ ] 2.06 → [✅] 2.06
   Note: principles.hash integrity tests added.
         SHA-256 algorithm verified. Tamper detection confirmed.
         Tests in tests/test_security.py.

5. MARK COMPLETE — Item 2.07
   Change: [ ] 2.07 → [✅] 2.07
   Note: pip-audit added to CI workflow.
         Scans requirements-test.txt on every push.
         ONTO has zero vulnerable dependencies confirmed.

6. MARK COMPLETE — Item 2.11
   Change: [ ] 2.11 → [✅] 2.11
   Note: core/config.py — all config loads from environment variables.
         .env.example published — documents every variable.
         .env in .gitignore — secrets cannot be committed.
         Tests in tests/test_security.py.

7. UPDATE CURRENT STATUS SECTION
   Replace the current status block at the bottom with this:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT STATUS — Version 1.0 MVP

[✅] Principles written, sealed, and hash-verified
[✅] Core modules written and syntax-verified
[✅] Full loop tested end to end
[✅] Safety flags tested — all three patterns pass
[✅] Memory append-only triggers confirmed
[✅] MIT license applied
[✅] Repository published publicly
[✅] Preservation document written
[✅] Project record document created
[✅] README complete with install instructions
[✅] Gist published — 3 sealed hashes recorded
[✅] 1.01 — Test suite — 136 tests, all passing
[✅] 1.02 — Principle verification tested
[✅] 1.03 — Safety flag detection tested
[✅] 1.04 — Memory append-only enforcement confirmed
[✅] 1.05 — Full five-step loop tested end to end
[✅] 1.06 — Type hints added to all 7 modules
[✅] 1.07 — Input sanitization layer in intake.py
[✅] 1.08 — Edge case and sanitization tests — 108 base tests
[✅] 1.09 — CHANGELOG.md — full history documented
[✅] 1.10 — .flake8 linting config
[✅] 1.11 — GitHub Actions CI — green across Python 3.8–3.12
[✅] 1.12 — Conformance tests — 16 tests, ratification complete
[✅] 2.03 — Input sanitization against injection attacks
[✅] 2.05 — Rate limiting — core/ratelimit.py
[✅] 2.06 — principles.hash protection — tests in place
[✅] 2.07 — Dependency scanning — pip-audit in CI
[✅] 2.11 — Environment variable config — core/config.py
[✅] 3.05 — CONTRIBUTING.md
[✅] 5.05 — Line endings protection
[✅] NEW — CROSSOVER_CONTRACT_v1.0 sealed on GitHub
[✅] NEW — CRE-SPEC-001 v0.6 sealed on GitHub
[✅] NEW — Gist updated — all three document hashes recorded

RULE: 1.09A — Code, tests, and documentation must always agree.

Tests: 136 passing across Python 3.8–3.12
CI: green ✅
Ratification: all 3 conditions met ✅

Remaining items: 31

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This document is part of the permanent record of ONTO.
It is updated as items are completed.
It is honest about what is not yet done.
That is the only way trust is built.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
